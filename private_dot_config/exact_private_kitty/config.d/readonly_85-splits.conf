enabled_layouts splits,stack

# Window navigation (vim-style, kitty_mod = ctrl+shift)
map kitty_mod+h neighboring_window left
map kitty_mod+l neighboring_window right
map kitty_mod+k neighboring_window up
map kitty_mod+j neighboring_window down

# Splits (matching tmux defaults)
map kitty_mod+2 launch --location=hsplit --cwd=current
map kitty_mod+5 launch --location=vsplit --cwd=current

# Resize
map kitty_mod+alt+h resize_window narrower
map kitty_mod+alt+l resize_window wider
map kitty_mod+alt+k resize_window taller
map kitty_mod+alt+j resize_window shorter

# Zoom (toggle focused window fullscreen within tab)
map kitty_mod+f toggle_layout stack
