# Allow 44.1 kHz for legacy Wine/DirectSound games such as FFXI.
# Without this, FFXI may open at 48 kHz and play music too fast.
context.properties = {
    default.clock.allowed-rates = [ 44100 48000 ]
}
