[Service]
# Vicinae is started as a Niri session component and therefore does not inherit
# Niri's KDL environment. Its launched applications should use the same KDE
# integration and application-menu layout as applications spawned by Niri.
Environment=QT_QPA_PLATFORMTHEME=kde
Environment=XDG_MENU_PREFIX=plasma-
