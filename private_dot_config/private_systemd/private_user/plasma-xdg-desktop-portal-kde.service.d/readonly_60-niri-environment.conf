[Service]
# The KDE portal is D-Bus/systemd activated, so it does not inherit Niri's KDL
# environment. Load Plasma's Qt integration to match applications such as Dolphin.
Environment=QT_QPA_PLATFORMTHEME=kde
