// Zed keymap - managed by chezmoi.
[
  {
    "context": "vim_mode == normal && !menu",
    "bindings": {
      "space f b": "tab_switcher::ToggleAll",
      "space f f": "file_finder::Toggle",
      "space /": "text_finder::Toggle"
    }
  }
]

// vim: set ft=jsonc:
