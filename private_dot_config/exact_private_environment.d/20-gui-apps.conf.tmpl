{{- $xdg := lower (env "XDG_CURRENT_DESKTOP") -}}
# QT/KDE theming
{{- if not (contains $xdg "kde") }}
QT_QPA_PLATFORMTHEME=kde
{{- end }}

# Electron apps - auto-detect
ELECTRON_OZONE_PLATFORM_HINT=auto

# 1Password locale workaround
OP_LOCALE=en_US
