{{ if ne .chezmoi.os "darwin" }}
{{ if eq .chezmoi.osRelease.id "fedora" }}
# prefer the built-in CCID driver so gpg-agent owns the reader and caches PINs properly
# (leaving disable-ccid unset keeps pcscd out of the way)
# let the card relock itself after 10 minutes of inactivity
card-timeout 600
{{ end }}
{{ end }}
