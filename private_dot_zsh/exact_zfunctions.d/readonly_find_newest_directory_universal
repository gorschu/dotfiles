function find_newest_directory_universal() {
    if (( $# == 0 )); then
        dir='.'
    else
        dir=$1
    fi
    find "${dir}" -type d -printf '%T@ %p\n' | sort -n | tail -1 | cut -f2- -d" "
}

# vim: ft=sh
