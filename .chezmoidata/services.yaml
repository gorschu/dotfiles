services:
  systemd:
    start_only:
      - syncthing.service
    enable_restart:
      - atuin.service
    enable_start:
      - app-com.mitchellh.ghostty.service
      - podman.socket
      - podman-auto-update.timer
