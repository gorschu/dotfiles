[Unit]
Description=Btrfs root subvolume mount for btrbk

[Mount]
What={{ btrbk_device }}
Where=/mnt/btrfs_root
Type=btrfs
Options=subvolid=5,noatime
DirectoryMode=0755

[Install]
WantedBy=multi-user.target
