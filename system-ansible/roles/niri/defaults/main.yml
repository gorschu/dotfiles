---
# Niri role defaults

# State of Niri installation: present or absent
niri_state: present

# Use DankMaterialShell as the desktop shell
# When true: installs DMS (all-in-one desktop shell)
# When false: installs traditional individual components
niri_use_dms: true

# Package lists
niri_core_packages:
  - niri            # Core compositor
  - udiskie         # USB automounting
  - xwayland-satellite  # X11 app support for Wayland
  - pinentry-qt     # Better GPG passphrase dialog
  - plasma-breeze-qt5   # Qt5 theme
  - plasma-breeze-qt6   # Qt6 theme

niri_traditional_packages:
  - fuzzel          # Application launcher
  - mako            # Notification daemon
  - waybar          # Status bar
  - hyprlock        # Screen lock
  - hypridle        # Idle management
  - hyprpaper       # Wallpaper manager

niri_dms_packages:
  - dms             # DankMaterialShell
