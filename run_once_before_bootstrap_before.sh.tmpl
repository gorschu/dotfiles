#!/bin/bash

set -Eeuxo pipefail

red=$(tput setaf 1)
green=$(tput setaf 2)
reset=$(tput sgr0)

echo "${green}Installing requirements${reset}"
sudo dnf install -y git zsh redhat-lsb-core yubikey-manager python3-pip pipx

