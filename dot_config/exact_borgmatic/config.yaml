---
# WARNING: Do not edit this file.
# It was generated by processing /home/azmo/.config/borgmatic/config.yaml##t.j2
location:
    source_directories:
        - /home/azmo

    repositories:
        - ~/backup

    one_file_system: true

    numeric_owner: false
    atime: true
    ctime: true
    birthtime: true

    read_special: false
    bsd_flags: true

    files_cache: ctime,size,inode
    exclude_patterns:
        - '*.iso'
        - '/home/*/.cache'
        - '/home/*/.local/share/JetBrains'
        - '/home/*/.config/nvim/plugged'
        - '/home/*/.config/emacs'
        - '/home/*/.config/google-chrome'
        - '/home/*/.npm'
        - '/home/*/.config/Code/Cache*'
        - '/home/*/.vagrant.d'
        - '/home/*/.vscode'
        - '/home/*/backup'

    exclude_caches: true

storage:
    encryption_passcommand: pass show admin/backup/hephaestus/azmo-home
    checkpoint_interval: 600

    compression: zstd

retention:
    keep_within: '24H'
    keep_hourly: 24
    keep_daily: 7
    keep_weekly: 4
    keep_monthly: 6
    keep_yearly: 1

consistency:
    checks:
        - repository
        - archives

output:
    color: true
