#!/bin/bash

# anything LC_something trips over FFXIV Launcher's wine implementation
# keys are sometimes wrong, therefore undefine
for nogerman in $(env | grep "LC_" | cut -d "=" -f 1); do
  unset -v "${nogerman}"
done

flatpak run dev.goats.xivlauncher
